# 飞书应用创建指南（一次性配置）

本系统通过**飞书自建应用 + WebSocket 长连接**接收消息与卡片回调。
以下是完整配置步骤，约 10 分钟。

## 1. 创建自建应用

1. 打开 [飞书开放平台](https://open.feishu.cn/app)（个人飞书可直接登录）。
2. 点 **创建企业自建应用**（个人租户也走这个入口）。
3. 填写名称（如 `remenberall`）、描述，创建完成。
4. 记录 **App ID** 和 **App Secret**，填入 `.env` 的 `FEISHU_APP_ID` / `FEISHU_APP_SECRET`。

## 2. 开启机器人能力

1. 左侧 **添加应用能力 → 机器人**，点启用。
2. 这样用户才能私聊它、它能发消息。

## 3. 申请权限

左侧 **权限管理**，搜索并开通以下权限：

| 权限 | 用途 |
|---|---|
| `im:message` | 接收/发送消息 |
| `im:message:send_as_bot` | 以机器人身份发消息 |
| `docx:document` | 创建/写入备份文档 |
| `im:message.p2p_msg:readonly`（如列出） | 接收私聊消息 |

> 若个别权限名称在控制台里对不上，以「发送消息」「接收消息」这类中文说明为准，
> 对应的是 `im:message` 系列。

申请权限后，**发布版本**（版本管理与发布 → 创建版本 → 提交发布）。
自建应用在自己租户内一般可立即生效。

## 4. 事件订阅（关键：用「长连接」模式）

左侧 **事件与回调**：

1. 订阅方式选 **使用长连接接收事件**（不需要公网回调地址，本地/服务器都能跑）。
2. 添加事件：
   - `im.message.receive_v1` —— 收到用户消息
   - `card.action.trigger` —— 用户点卡片按钮（卡片回传交互）
3. 保存。事件订阅中的「Verification Token」「Encrypt Key」不需要填（长连接模式用不到）。

> 如果控制台默认要填回调地址：请确认是否先选了「长连接」；
> 只有选长连接后才会出现事件列表，且不需要公网 URL。

## 5. 把自己加到应用可见范围

左侧 **权限管理 → 可用范围**，确认包含你自己的账号（个人租户默认全可见）。

## 6. 验证

启动 daemon 后（`python -m app.cli daemon`），在飞书里私聊你的机器人：

- 发一句「你好」→ 应收到系统回复或「提炼失败」提示（说明链路通了）
- `python -m app.cli push-test` → 应收到测试消息

## 常见问题

- **机器人收不到消息**：检查是否发布了版本、事件是否订阅成功、是否长连接模式。
- **卡片按钮点了没反应**：检查 `card.action.trigger` 事件是否已订阅，日志里是否有回调记录。
- **提示权限不足**：回控制台补权限并重新发布版本。

## 备注

- 飞书 SDK（`lark-oapi`）的长连接会自动断线重连。
- 若你所在的企业租户禁止自建应用，可改用**自定义机器人 webhook**（只推送、不支持按钮反馈），
  或使用个人飞书账号（本系统默认按个人飞书设计）。
